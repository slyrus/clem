
(in-package :clem)

(declaim (inline mref))
  
(defmacro def-matrix-mref (type)
  (let ((element-type (element-type (find-class `,type)))
        (fast-mref-symbol (ch-util:make-intern
                           (concatenate 'string (symbol-name `,type) "-mref"))))
    `(progn
       (declaim (ftype (function (,type fixnum fixnum) ,element-type)
                       ,fast-mref-symbol))
       (declaim (inline ,fast-mref-symbol))
       (defun ,fast-mref-symbol (m row col)
	 (with-typed-matrix-vals (m ,element-type a)
	   (aref a row col)))
       
       (declaim (ftype (function (,element-type ,type fixnum fixnum) ,element-type)
                       (setf ,fast-mref-symbol)))
       (declaim (inline (setf ,fast-mref-symbol)))
       (defun (setf ,fast-mref-symbol) (v m row col)
	 (with-typed-matrix-vals (m ,element-type a)
	   (setf (aref a row col) v)))
       
       (defmethod mref ((m ,type) (row fixnum) (col fixnum))
	 (with-typed-matrix-vals (m ,element-type a)
	   (aref a row col)))
       
       (defmethod (setf mref) (v (m ,type) (row fixnum) (col fixnum))
	 (with-typed-matrix-vals (m ,element-type a)
	   (setf (aref a row col) v))))))

(macrolet ((frob (type)
	     `(def-matrix-mref ,type)))
  (frob double-float-matrix)
  (frob single-float-matrix)
  (frob ub8-matrix)
  (frob ub16-matrix)
  (frob ub32-matrix)
  (frob sb8-matrix)
  (frob sb16-matrix)
  (frob sb32-matrix)
  (frob fixnum-matrix)
  (frob real-matrix)
  (frob integer-matrix)
  (frob complex-matrix)
  (frob bit-matrix))

(define-compiler-macro mref (m row col)
  (typecase m
    (double-float-matrix
     (print 'shit)
     `(with-typed-matrix-vals (,m double-float a)
        (declare type (simple-array double-float (* *)) a)
        (aref a ,row ,col)))
    (t
     (print (type-of m))
     `(aref (matrix-vals ,m) ,row ,col))))


(sb-c:defknown mref2 (matrix fixnum fixnum) number
  (sb-c:flushable sb-c:movable))

#+nil
(sb-c:deftransform mref2 ((m row col))
  (print (sb-c::classoid-name (sb-c::lvar-type m)))
  (case (sb-c::classoid-name (sb-c::lvar-type m))
    (double-float-matrix
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array double-float (* *)) a))
        (aref a row col)))
    (single-float-matrix
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array single-float (* *)) a))
        (aref a row col)))
    (single-float-matrix
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array single-float (* *)) a))
        (aref a row col)))
    (t
     `(aref (matrix-vals m) row col))))

(sb-c:deftransform mref2 ((m row col))
  (print (sb-c::lvar-type m))
  (case (sb-c::lvar-type m)
    (#.(sb-c::find-classoid 'double-float-matrix)
       `(let ((a (matrix-vals m)))
          (declare (type (simple-array double-float (* *)) a))
          (aref a row col)))
    (#.(sb-c::find-classoid 'single-float-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array single-float (* *)) a))
        (aref a row col)))
    (#.(sb-c::find-classoid 'real-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array real (* *)) a))
        (aref a row col)))
    (#.(sb-c::find-classoid 'complex-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array complex (* *)) a))
        (aref a row col)))
    (#.(sb-c::find-classoid 'ub8-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (unsigned-byte 8) (* *)) a))
        (aref a row col)))
    (#.(sb-c::find-classoid 'ub16-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (unsigned-byte 16) (* *)) a))
        (aref a row col)))
    (#.(sb-c::find-classoid 'ub32-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (unsigned-byte 32) (* *)) a))
        (aref a row col)))
    (#.(sb-c::find-classoid 'sb8-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (signed-byte 8) (* *)) a))
        (aref a row col)))
    (#.(sb-c::find-classoid 'sb16-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (signed-byte 16) (* *)) a))
        (aref a row col)))
    (#.(sb-c::find-classoid 'sb32-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (signed-byte 32) (* *)) a))
        (aref a row col)))
    (#.(sb-c::find-classoid 'bit-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (unsigned-byte 1) (* *)) a))
        (aref a row col)))
    (t
     `(aref (matrix-vals m) row col))))

(sb-c:defknown (setf mref2) (number matrix fixnum fixnum) number
  (sb-c:flushable sb-c:movable))

(sb-c:deftransform (setf mref2) ((val m row col))
  (print (sb-c::lvar-type m))
  (case (sb-c::lvar-type m)
    (#.(sb-c::find-classoid 'double-float-matrix)
       `(let ((a (matrix-vals m)))
          (declare (type (simple-array double-float (* *)) a))
          (setf (aref a row col) val)))
    (#.(sb-c::find-classoid 'single-float-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array single-float (* *)) a))
        (setf (aref a row col) val)))
    (#.(sb-c::find-classoid 'real-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array real (* *)) a))
        (setf (aref a row col) val)))
    (#.(sb-c::find-classoid 'complex-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array complex (* *)) a))
        (setf (aref a row col) val)))
    (#.(sb-c::find-classoid 'ub8-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (unsigned-byte 8) (* *)) a))
        (setf (aref a row col) val)))
    (#.(sb-c::find-classoid 'ub16-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (unsigned-byte 16) (* *)) a))
        (setf (aref a row col) val)))
    (#.(sb-c::find-classoid 'ub32-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (unsigned-byte 32) (* *)) a))
        (setf (aref a row col) val)))
    (#.(sb-c::find-classoid 'sb8-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (signed-byte 8) (* *)) a))
        (setf (aref a row col) val)))
    (#.(sb-c::find-classoid 'sb16-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (signed-byte 16) (* *)) a))
        (setf (aref a row col) val)))
    (#.(sb-c::find-classoid 'sb32-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (signed-byte 32) (* *)) a))
        (setf (aref a row col) val)))
    (#.(sb-c::find-classoid 'bit-matrix)
     `(let ((a (matrix-vals m)))
        (declare (type (simple-array (unsigned-byte 1) (* *)) a))
        (setf (aref a row col) val)))
    (t
     `(setf (aref (matrix-vals m) row col) val))))


